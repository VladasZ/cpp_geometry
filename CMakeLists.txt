cmake_minimum_required(VERSION 3.8 FATAL_ERROR)

GET_PROPERTY(GEOMETRY_ADDED_LOCAL GLOBAL PROPERTY GEOMETRY_ADDED_GLOBAL)

if(GEOMETRY_ADDED_LOCAL EQUAL 1)
  return()
endif()

SET_PROPERTY(GLOBAL PROPERTY GEOMETRY_ADDED_GLOBAL 1)

project(geometry)

add_catalog_recursive(${PROJECT_SOURCE_DIR} / SOURCE)
add_library(${PROJECT_NAME} ${SOURCE})
